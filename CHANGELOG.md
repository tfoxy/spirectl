# Changelog

What changed in spirectl, written for the people building against it — the `sts2` CLI, the npm
wrapper, `Spirectl.Sts2`, and the embeddable runtime. Each GitHub Release carries the section below
it verbatim.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and versions follow
[semantic versioning](https://semver.org/spec/v2.0.0.html). Sections are drafted from the
`Changelog:` trailers on commits — see [docs/commit-and-release.md](docs/commit-and-release.md).

## [Unreleased]

First release not yet cut. `0.1.0` is declared in `cli/Cargo.toml`,
`bridge-mod/Directory.Build.props` and `npm-wrapper/package.json`; tagging it publishes the CLI
downloads, the npm wrapper and the `Spirectl.Sts2` NuGet package — see
[docs/releasing.md](docs/releasing.md).

[Unreleased]: https://github.com/tfoxy/spirectl/commits/main
