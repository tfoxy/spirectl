schemaVersion: spirectl.fixture/v0
name: multiplayer-ownership-local-only-degraded
description: Local-only degraded multiplayer lobby slice with remote client metadata omitted from execution.
characterSelect:
  kind: load-run
  lobby:
    seed: fixture-multiplayer-ownership-local-only-degraded
    hostPlayerId: "p:100"
    players:
      - id: "p:100"
        characterId: IRONCLAD
        isReady: false
      - id: "p:200"
        characterId: SILENT
        isReady: true
